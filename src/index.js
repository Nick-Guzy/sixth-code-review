import Person from "./age";

function handleAgeForm(event) {
  window.addEventListener("load", function());
    addEventListener("click", handleAgeForm);
  event.preventDefault();
  const currentAge = document.querySelector("age-checker-form").value;
  const pastAge = 
  myPerson = new Person(currentAge, pastAge, futureAge);
  const calcAge = myPerson.checkType();

}
